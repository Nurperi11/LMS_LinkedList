package db;

import models.Actor;
import models.Movie;
import models.Producer;

import java.util.LinkedList;
import java.util.List;

public class Database {
    public static List<Movie> movieList = new LinkedList<>();
    public static List<Actor> actorList = new LinkedList<>();
    public static List<Producer> producerList = new LinkedList<>();
}
